def issubsum(arr, sum):
    s, curr = 0, 0
    for i in range(len(arr)):
        curr += arr[i]
        while curr > sum:
            curr -= arr[s]
            s += 1
        if curr == sum:
            return True

    return False


arr = [-100, 2, 3, 4, 5]
sum = 9
print(issubsum(arr, sum))
